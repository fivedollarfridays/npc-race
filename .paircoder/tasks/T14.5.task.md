---
id: T14.5
title: "Integration gate — sound verification"
plan: plan-2026-03-npc-race-sound
status: todo
complexity: 10
depends_on: [T14.4]
---

# T14.5 — Integration Gate: Sound Verification

## Objective

Verify the sound overhaul is wired end-to-end. All new audio features exist in the JS, are referenced from main.js, and the HTML loads everything correctly.

## Tests: `tests/test_sound_overhaul.py`

**Class TestSpatialAudio:**
- `test_sound_engine_has_spatial_functions` — initSpatialAudio, updateSpatialAudio present
- `test_sound_engine_has_car_slots` — _carAudioSlots referenced

**Class TestTurboWhisle:**
- `test_turbo_whistle_exists` — triggerTurboWhistle in sound-engine.js

**Class TestDramaCrowd:**
- `test_drama_score_in_sound_engine` — updateDramaScore present
- `test_events_wired_in_main` — main.js references events/commentary from replay

**Class TestCameraMix:**
- `test_camera_mix_in_sound_engine` — setCameraMix present
- `test_mix_profiles` — _mixProfiles in sound-engine.js

**Class TestArchCompliance:**
- `test_sound_engine_under_limits` — sound-engine.js ≤ 350 lines
- `test_simulation_unchanged` — simulation.py ≤ 400 lines, ≤ 15 functions

## Acceptance Criteria

- ≥ 1375 fast tests passing
- All new audio features exist and are wired
- All arch limits respected
- Sprint 14 complete
