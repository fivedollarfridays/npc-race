---
id: T34.8
title: "Grid export + race integration + CLI pipeline"
plan: plan-2026-03-sprint34-fast-sim
status: todo
complexity: 3
depends_on: [T34.7]
---

# T34.8 — Grid Export + Race Pipeline

Wire qualifying into race flow. Write grid.json, read for start order.

## CLI
```
npcrace qualify --car-dir cars --track monza
npcrace race --car-dir cars --track monza --qualify
```

## Implementation
1. `engine/qualifying.py`: add `export_grid(results, path)`
2. `engine/race_runner.py`: add `grid_file` param, reorder cars
3. `cli/main.py`: add qualify + race subcommands
4. `cli/commands.py`: cmd_qualify, cmd_race (pipeline)

## AC
- [ ] `npcrace qualify` writes grid.json
- [ ] `npcrace run` reads grid.json if present
- [ ] `npcrace race --qualify` runs Q → race → leaderboard
- [ ] P1 qualifier starts at front
- [ ] Integration test covers full flow
