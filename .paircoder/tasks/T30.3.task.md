---
id: T30.3
title: "Wire into race runner + CLI"
plan: plan-2026-03-sprint30-league-system
status: todo
complexity: 10
depends_on: [T30.2]
---

# T30.3 — Wire Into Race Runner

Update race_runner.py and CLI to accept league parameter.

## Changes
- race_runner.py run_race(): accept league param, validate cars before racing
- cli/commands.py cmd_run(): add --league flag (default: auto-detect)
- Auto-detect: count _loaded_parts → determine_league()
- Print quality report before race starts
- Skip cars that fail enforced gates (print rejection reason)

## AC
- `npcrace run --league F3` validates cars for F3
- Auto-detect works: 3-part car → F3 without --league flag
- Quality report printed to console before race
- Failed cars skipped with clear message
