---
id: T2.11
title: Tests for replay enrichment and build system
plan: plan-2026-03-npc-race-viewer
type: feature
priority: P0
complexity: 25
status: pending
sprint: '2'
tags: [testing]
depends_on: [T2.1, T2.2]
---

# Objective

Write tests for the replay data enrichment (T2.1) and the viewer build system (T2.2).

# Implementation Plan

1. Extend `tests/test_replay.py`:
   - Test `compute_track_headings` returns list of correct length
   - Test `compute_track_headings` values are in [-pi, pi] range
   - Test `compute_track_headings` on known shape (e.g., square) returns expected angles
   - Test exported replay contains `track_curvatures` array with length == len(track)
   - Test exported replay contains `track_headings` array with length == len(track)
   - Test each car frame dict contains `seg` as integer in range [0, len(track))

2. New `tests/test_build_viewer.py`:
   - Test `scripts/build_viewer.py` runs without error
   - Test output `viewer.html` exists after build
   - Test output contains `<script>` tags (JS was inlined)
   - Test output contains NO `<!-- INJECT:` markers
   - Test output starts with `<!DOCTYPE html>` and ends with `</html>`
   - Test output contains key strings ("NPC Race", "replay.json", "togglePlay")

# Acceptance Criteria

- [ ] All new replay enrichment tests pass
- [ ] All new build system tests pass
- [ ] Existing tests still pass (no regressions)
- [ ] Total new tests: ~10-12

# Verification

- `pytest tests/test_replay.py tests/test_build_viewer.py -v`
- `python -m pytest --tb=short -q` (full suite)
- `ruff check tests/`
