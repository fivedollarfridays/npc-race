---
id: T37.1
title: "cmd_init: create inside cars/, set CAR_NAME, validate name"
plan: plan-2026-03-sprint37-onboarding-bugfixes
status: done
complexity: 3
depends_on: []
---

# T37.1 — Fix cmd_init

Three fixes in one:
1. Create project inside `cars/{name}/` not `./{name}/`
2. Set `CAR_NAME` to PascalCase of dir name (not "DefaultProject")
3. Reject absolute paths and path separators

## AC
- [ ] `npcrace init my_car` creates `cars/my_car/` with `CAR_NAME = "MyCar"`
- [ ] `npcrace init /tmp/foo` returns error
- [ ] Existing tests updated to match new behavior
