---
id: T34.6
title: "Precompute track invariants"
plan: plan-2026-03-sprint34-fast-sim
status: todo
complexity: 2
depends_on: []
---

# T34.6 — Precompute Track Invariants

Replace per-tick linear scan `get_curvature_at` with bisect-based lookup.

## Implementation
1. Add `CurvatureLookup` class in `engine/track_gen.py`
2. Precompute bisect-ready array from distances/curvatures
3. O(log n) lookup via `__getitem__(distance)`
4. Build in `RaceSim.__init__`, replace calls in sim_physics.py

## AC
- [ ] Curvature values identical to linear scan
- [ ] Uses bisect (O(log n))
- [ ] All physics tests pass unchanged
