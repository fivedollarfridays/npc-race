---
id: T1.12
title: Full test suite + arch check
plan: plan-2026-03-npc-race-get-sellable
type: feature
priority: P1
complexity: 5
status: done
sprint: '1'
tags: [testing, quality]
depends_on: [T1.5, T1.6, T1.8]
---

# Objective

Run the full pytest suite and confirm all tests pass (should be 200+ after T1.5, T1.6, T1.8 are complete). Run `bpsai-pair arch check` on all source files and fix any violations.

# Implementation Plan

1. Run `pytest -v` and confirm all tests pass
2. Run arch check on all source files
3. Fix any failing tests or arch violations
4. Confirm final count is 200+ tests all green

# Acceptance Criteria

- [ ] All tests green (should be 200+ after T1-T3)
- [ ] `bpsai-pair arch check` passes on all source files
- [ ] No ruff/lint violations

# Verification

- `pytest -v`
- `bpsai-pair arch check src/` (or equivalent source dirs)
- `ruff check .`
