---
id: T11.6
title: "Integration gate — ERS + brake verification"
plan: plan-2026-03-npc-race-ers-brakes
status: todo
complexity: 15
depends_on: [T11.5]
---

# T11.6 — Integration Gate: ERS + Brake Verification

## Objective

Verify ERS and brake temp work end-to-end. Run races and confirm energy depletes, brakes heat up, and everything integrates with existing systems.

## Tests: `tests/test_ers_brake_integration.py`

Mark with `@pytest.mark.slow`.

**Class TestERSIntegration:**
- `test_ers_energy_in_replay` — replay frames have "ers_energy" float
- `test_ers_depletes_during_race` — some frame has ers_energy < 4.0
- `test_ers_mode_affects_speed` — (unit test) attack mode car vs harvest mode, attack should be faster

**Class TestBrakeTempIntegration:**
- `test_brake_temp_in_replay` — replay frames have "brake_temp" float
- `test_brakes_heat_during_race` — some frame has brake_temp > 50 (above ambient)
- `test_brake_fade_reduces_braking` — (unit test) high brake temp → efficiency < 1.0

**Class TestArchCompliance:**
- `test_simulation_under_limits` — simulation.py ≤ 400 lines
- `test_simulation_function_count` — ≤ 15 functions
- `test_new_modules_under_limits` — ers_model.py ≤ 100, brake_model.py ≤ 90, drama.py ≤ 80

## Acceptance Criteria

- ≥ 1320 tests passing (fast suite)
- ERS energy depletes during races
- Brake temp rises under braking
- All arch limits respected
- Sprint 11 complete
