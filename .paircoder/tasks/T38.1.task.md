---
id: T38.1
title: "Bot scanner + docs + env fixes (Critical #1-3)"
plan: plan-2026-03-sprint38-audit-bugfixes
status: todo
complexity: 3
depends_on: []
---

# T38.1 — Critical Fixes

## Bug 1: Bot scanner rejects init'd cars
`security/bot_scanner.py:_parse_top_level_assignments` only handles
`ast.Name` targets. Template uses tuple unpacking:
`POWER, GRIP, WEIGHT, AERO, BRAKES = 20, 20, 20, 20, 20`

Fix: Handle `ast.Tuple` targets in the AST parser.

## Bug 2: GETTING_STARTED.md wrong command
Line 17: `npcrace run --car-dir my_car` should be `--car-dir cars`
Also fix line 28 (first improvement section).

## Bug 3: Editable install collision
Add `npc_race.egg-info/` to `.gitignore` if not already there.
Document the fix in GETTING_STARTED.md: `pip install -e .` must
be run from the npc-race directory.

## AC
- [ ] `npcrace validate cars/default_project/car.py` passes
- [ ] `npcrace init test && npcrace validate cars/test/car.py` passes
- [ ] GETTING_STARTED.md commands work when followed literally
