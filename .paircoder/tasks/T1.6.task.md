---
id: T1.6
title: play.py -- add --track and --list-tracks flags
plan: plan-2026-03-npc-race-v1
type: feature
priority: P1
complexity: 25
status: done
sprint: '2'
tags:
- cli
depends_on:
- T1.5
completed_at: '2026-03-17T00:26:58.304636'
---

# Objective

Update play.py CLI to support named track selection and track listing.

# Implementation Plan

1. Add `--track NAME` argument to argparse
2. Add `--list-tracks` flag that prints available tracks and exits
3. When --track specified, pass track_name to run_race
4. When --track "random", pick a random track
5. When --track specified, ignore --seed
6. Update help text
7. Write tests

# Acceptance Criteria

- [x] `python play.py --track monza` runs race on Monza
- [x] `python play.py --track random` picks random track
- [x] `python play.py --list-tracks` prints all 20 tracks with name, country, character
- [x] `python play.py --seed 42` still works (procedural, no track_name)
- [x] --track ignores --seed when both specified
- [x] Invalid --track name gives clear error message
- [x] Tests cover all flag combinations

# Verification

- `pytest tests/test_play.py -v`
- `python play.py --list-tracks` shows 20 tracks
- `python play.py --track silverstone --no-browser` produces valid replay