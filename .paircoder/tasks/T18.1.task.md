---
id: T18.1
title: "Fix WS render pipeline + speed chart + timing tower + overlay"
plan: plan-2026-03-npc-race-viewer-bugfix
status: todo
complexity: 30
depends_on: []
---

# T18.1 — Viewer Bugfix

## Fixes

### 1. WS render pipeline (main.js handleWsFrame)
- Always call `computeTransform()` and `renderBackground()` on every WS frame
- Remove the `if (cameraSystem.mode !== 'full')` guard in handleWsFrame
- Defer `enrichReplayData()` until first frame arrives (not in handleWsInit)

### 2. Dynamic speed chart Y-axis (telemetry-strip.js)
- Replace hardcoded `yMax = 380` with dynamic scaling
- Track max observed speed, use `Math.max(maxSpeed * 1.1, 100)` as Y max

### 3. Add speed to timing tower (timing-tower.js)
- Add `.tower-speed` element to row template
- Update speed display in `updateTimingTower()`

### 4. Fix overlay speed readout (overlay.js)
- Show selected car speed if available, fall back to leader

## Acceptance Criteria
- Track visible in WS mode
- Cars render within track bounds
- Speed chart scales dynamically
- Timing tower shows per-car speeds
- All existing tests pass
