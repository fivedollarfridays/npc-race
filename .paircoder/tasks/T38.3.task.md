---
id: T38.3
title: "Output cleanup (Medium #7-10)"
plan: plan-2026-03-sprint38-audit-bugfixes
status: todo
complexity: 3
depends_on: []
---

# T38.3 — Output Cleanup

## Bug 7: Results displayed twice
`race_runner._print_results` prints a RESULTS block, then
`race_dashboard.generate_dashboard` prints RACE RESULTS.
Remove `_print_results` — dashboard is the canonical output.

## Bug 8: replay.json in .gitignore
Add `replay.json`, `results.json`, `lap_summary.json`, `grid.json`
to `.gitignore` so new players don't commit 56MB files.

## Bug 9: Submit time format
`cli/data_commands.py:_print_submit_summary` shows raw seconds.
Format as mm:ss.fff to match race output.

## Bug 10: Cars listed twice with --qualify
`cmd_race` calls qualify then run — both print car loading.
Suppress car loading output on the second call, or load once.

## AC
- [ ] Only one results table after a race (dashboard only)
- [ ] `.gitignore` includes replay/results/lap_summary/grid json
- [ ] `npcrace submit` shows times as mm:ss.fff
- [ ] `npcrace race --qualify` lists cars only once
