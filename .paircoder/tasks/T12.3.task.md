---
id: T12.3
title: "Seed cars + template docs"
plan: plan-2026-03-npc-race-info-asymmetry
status: todo
complexity: 15
depends_on: [T12.2]
---

# T12.3 — Seed Cars + Template Docs

## Objective

Update car_template.py to document the information asymmetry model and `opponent_info`. Update 1-2 seed cars to use `opponent_info` for inference-based strategy.

## car_template.py documentation

Add after Sprint 11 section:
```python
# Sprint 12: Information Asymmetry
# state["opponent_info"]  list  All opponents with observable-only data:
#   name, position, speed, lateral, tire_compound, tire_age_laps,
#   pit_stops, drs_active, in_spin, finished, gap_ahead_s, lap
# HIDDEN from opponents: tire_wear, fuel, damage, tire_temp, engine_mode,
#   ers_energy, brake_temp, spin_risk — only YOUR car has these.
```

## Seed car updates

**GooseLoose** — inference from opponent lap times:
- Use `opponent_info` to check if car ahead has high tire_age_laps (>15) → likely worn tires → push harder to overtake
- If opponent pit_stops == 0 and lap > total_laps/2 → they'll pit soon, don't waste ERS attacking

**SlipStream** — draft target selection:
- Use `opponent_info` to find the fastest car ahead (highest speed) for optimal drafting position

## Tests

- `test_gooseloose_pushes_against_worn_opponent` — opponent tire_age > 15 → engine_mode "push"
- `test_slipstream_targets_fastest_for_draft` — checks nearby_cars usage still works

## Acceptance Criteria

- car_template.py documents opponent_info and information asymmetry
- 1-2 seed cars use opponent_info
- All seed cars pass bot_scanner
- All tests pass, ruff clean
