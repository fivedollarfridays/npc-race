---
id: T36.4
title: "Convert CLI/export/dashboard tests to use fixtures"
plan: plan-2026-03-sprint36-test-tiers
status: todo
complexity: 3
depends_on: [T36.3]
---

# T36.4 — Convert Tests to Fixtures

Tests for leaderboard, dashboard, CLI, export, and submission don't need
the real sim — they need the sim's output format. Convert them to use
fixtures from T36.3.

## Target files (tests that run sim but only test non-sim code)
- tests/test_race_dashboard.py — test dashboard formatting with fixture data
- tests/test_fast_export.py — test export with mock sim/accumulator
- tests/test_submission_pipeline.py — test submission with fixture results
- tests/test_leaderboard_cli.py — already doesn't run sim (verify)
- tests/test_results_export.py — test export with fixture data

## For each file
1. Replace `run_race()` / `RaceSim()` calls with fixture data
2. Mock the sim where needed
3. Verify the test still covers the same behavior
4. Ensure test runs in < 1s

## AC
- [ ] Converted tests run in < 1s each (from 5-15s)
- [ ] No sim calls in converted tests
- [ ] All assertions unchanged
- [ ] Test count unchanged
