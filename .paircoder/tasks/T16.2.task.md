---
id: T16.2
title: "Championship points system"
plan: plan-2026-03-npc-race-championship
status: todo
complexity: 15
depends_on: []
---

# T16.2 — Championship Points System

## Objective

Create `engine/championship.py` — tracks championship standings across multiple races using the F1 points system.

## Design

**Points per race:** 25/18/15/12/10/8/6/4/2/1 for P1-P10. DNFs get 0.

**Standings:** Accumulated points per car across all races. Tiebreaker: most wins, then most P2s, etc.

## New file: `engine/championship.py` (~70 LOC)

```python
F1_POINTS = [25, 18, 15, 12, 10, 8, 6, 4, 2, 1]

def create_standings() -> dict:
    """Return empty standings dict."""

def award_points(standings: dict, results: list) -> dict:
    """Award F1 points from a race result. Returns updated standings."""

def get_sorted_standings(standings: dict) -> list[tuple[str, int]]:
    """Return standings sorted by points (desc), tiebroken by wins."""

def format_standings(standings: dict) -> str:
    """Format standings as a printable table."""
```

## Tests: `tests/test_championship.py`

- `test_create_standings_empty` — starts empty
- `test_award_points_p1` — P1 gets 25 points
- `test_award_points_p10` — P10 gets 1 point
- `test_award_points_p11` — P11+ gets 0 points
- `test_cumulative_points` — points accumulate across races
- `test_sorted_standings` — sorted by points descending
- `test_tiebreaker_wins` — more wins breaks tie
- `test_format_standings` — returns non-empty string

## Acceptance Criteria

- `engine/championship.py` exists, ≤ 90 lines, ≤ 5 functions
- All 8 tests pass
- No simulation.py changes
