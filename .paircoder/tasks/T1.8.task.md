---
id: T1.8
title: viewer.html -- show track name in header
plan: plan-2026-03-npc-race-v1
type: feature
priority: P1
complexity: 15
status: done
sprint: '3'
tags: [viewer, frontend]
depends_on: [T1.5]
---

# Objective

Update viewer.html to display the track name from replay JSON in the header area.

# Implementation Plan

1. Read `track_name` from replay JSON (already added by T1.5)
2. Display track name in the header/title area
3. Fall back to "Custom Track" or "Procedural Track" if track_name is absent
4. Style consistent with existing dark theme

# Acceptance Criteria

- [x] Track name displayed in header when present in replay JSON
- [x] Graceful fallback when track_name missing (procedural races)
- [x] Styling consistent with existing viewer theme
- [x] No external dependencies added

# Verification

- Manual test: load a replay with track_name, verify display
- Manual test: load a replay without track_name, verify fallback
