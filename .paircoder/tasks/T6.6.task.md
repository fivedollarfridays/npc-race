---
id: T6.6
title: "Integration gate — realism verification"
plan: plan-2026-03-npc-race-realism
status: todo
complexity: 15
depends_on: [T6.5]
---

# T6.6 — Integration Gate: Realism Verification

## Objective

Verify Sprint 6 achieves realistic F1 physics and timing infrastructure works end-to-end. Run calibration races, verify lap times, check timing data in replays, ensure all quality gates pass.

## Implementation Plan

### New file: `tests/test_realism.py`

**Class `TestRealisticLapTimes`:**
- `test_monza_lap_time` — run 3-lap Monza, P1 lap time between 75-95 seconds
- `test_monaco_lap_time` — run 3-lap Monaco, P1 lap time between 65-85 seconds
- `test_car_spread_under_20_pct` — fastest car lap / slowest car lap < 1.20
- `test_no_speed_above_370` — no replay frame has speed > 370 km/h

**Class `TestRealisticTireWear`:**
- `test_soft_wear_after_5_laps` — run 5-lap Monza on softs, tire_wear between 0.15-0.40
- `test_tire_temp_reaches_operating_range` — by lap 3, at least 1 car has tire_temp between 70-110°C

**Class `TestRealisticFuel`:**
- `test_fuel_decreases_during_race` — fuel_pct at end < fuel_pct at start
- `test_fuel_pct_not_zero_after_5_laps` — still has fuel remaining after 5 laps

**Class `TestTimingInReplay`:**
- `test_elapsed_s_in_every_frame` — all frames have elapsed_s
- `test_results_have_timing_data` — results include total_time_s, best_lap_s, lap_times
- `test_lap_times_list_matches_lap_count` — len(lap_times) == laps for finished cars
- `test_best_lap_is_minimum_of_lap_times` — best_lap_s == min(lap_times)
- `test_gap_to_leader_in_frames` — gap_to_leader_s present in frames
- `test_sector_info_in_frames` — current_sector in [0, 1, 2] for all frames

**Class `TestArchCompliance`:**
- `test_simulation_under_limits` — simulation.py ≤ 320 lines, ≤ 15 functions
- `test_physics_module_under_limits` — physics.py ≤ 130 lines, ≤ 8 functions
- `test_timing_module_under_limits` — timing.py ≤ 120 lines, ≤ 8 functions

### Smoke tests (run via shell)

```bash
npcrace run --track monza --laps 3
npcrace run --track monaco --laps 3
npcrace tournament --tracks monza,silverstone --races 2 --laps 3
```

Verify console output shows formatted times (M:SS.mmm).

### Calibration script

Create `scripts/calibration_check.py` — runs 3-lap races on 5 tracks, prints:
- Lap times per car per track
- Top speeds per car
- Tire wear per lap
- Tire temps at end
- Fuel consumed

## Run List

1. `python -m pytest --tb=short -q --ignore=tests/test_balance_v2.py` — all tests pass
2. `ruff check .` — clean
3. `python scripts/calibration_check.py` — lap times and speeds in expected ranges
4. `npcrace run --track monza` — viewer shows realistic speeds and timing

## Acceptance Criteria

- ≥ 1020 tests passing (998 existing + 22+ new)
- Monza lap: 75-95 seconds
- Monaco lap: 65-85 seconds
- Max speed: ≤ 370 km/h
- Tire temp reaches 70-110°C by lap 3
- Fuel visibly consumed in replay
- Timing data (elapsed_s, lap_times, best_lap) in replay
- All arch limits respected
- Sprint 6 complete
