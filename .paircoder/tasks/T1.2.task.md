---
id: T1.2
title: Decompose engine.py into modules (<400 lines each)
plan: plan-2026-03-npc-race-v1
type: refactor
priority: P0
complexity: 50
status: pending
sprint: '1'
tags: [engine, architecture]
depends_on: []
---

# Objective

Split `engine.py` (currently 495 lines) into focused modules under 400 lines each, using hub-and-spoke pattern. This must preserve all existing behavior -- pure refactor, no feature changes.

# Implementation Plan

1. Read engine.py to identify logical groupings
2. Create `engine/` package:
   - `engine/__init__.py` -- hub, re-exports public API (RaceSim, run_race, generate_track)
   - `engine/physics.py` -- speed model, cornering, acceleration, braking, drafting, tire degradation, boost
   - `engine/track_gen.py` -- track generation (Catmull-Rom spline, procedural generation)
   - `engine/car_loader.py` -- car file loading, validation, stat checking
   - `engine/simulation.py` -- RaceSim class, race loop, frame recording
3. Move old `engine.py` to `engine_legacy.py` (backup), replace with package
4. Ensure `from engine import RaceSim, run_race` still works (backward compat)
5. Write/update tests for each module

# Acceptance Criteria

- [ ] engine.py replaced by engine/ package
- [ ] All module files under 400 lines
- [ ] `from engine import RaceSim, run_race` works (backward compatible)
- [ ] play.py runs without modification
- [ ] All existing tests pass (if any)
- [ ] Each module has single responsibility
- [ ] No circular imports

# Verification

- `python play.py --no-browser` runs successfully
- `pytest tests/ -v`
- `bpsai-pair arch check engine/`
